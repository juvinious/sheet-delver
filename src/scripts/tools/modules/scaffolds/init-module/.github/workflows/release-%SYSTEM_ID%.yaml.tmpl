name: Release Module

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write
    uses: sheetdelver/sheetdelver/.github/workflows/module-release.yml@%CORE_REF%
    with:
      module_id: '%SYSTEM_ID%'
      core_ref: '%CORE_REF%'
